#include <stdio.h>
#include <stdlib.h>

int main()
{
double result; /* The result of our calculation */
result= (double)(3+1)/(double)5; /* Need to double to get answer in decimals, float can also be used  */
printf("The value of 4/5 is %lf",result);
    return 0;
}
